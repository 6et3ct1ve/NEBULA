name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Той самий Dockerfile що й локально — середовище ідентичне
      - name: Run checks inside dev container
        uses: devcontainers/ci@v0.3
        with:
          push: never
          runCmd: |
            ruff format --check .
            ruff check .
            mypy .
            pytest